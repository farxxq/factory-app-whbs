import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CartonpackingGarmentscanPage } from './cartonpacking-garmentscan.page';

describe('CartonpackingGarmentscanPage', () => {
  let component: CartonpackingGarmentscanPage;
  let fixture: ComponentFixture<CartonpackingGarmentscanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CartonpackingGarmentscanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
